package dev.simplecore.simplix.stream.core.enums;

/**
 * Transport protocol type for stream connections.
 */
public enum TransportType {

    /**
     * Server-Sent Events (SSE) transport
     */
    SSE,

    /**
     * WebSocket with STOMP transport
     */
    WEBSOCKET
}
